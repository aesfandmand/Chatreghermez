# چتر قرمز — مخزن اسناد و صفحات

این مخزن، نسخهٔ پایهٔ اسناد حاکمیتی و صفحات ساخته‌شدهٔ پروژهٔ چتر قرمز است.
همهٔ فایل‌ها HTML مستقل و خودبسنده‌اند و با باز کردن در مرورگر کار می‌کنند.

## ساختار

| مسیر | فایل | نسخه | توضیح |
|---|---|---|---|
| `docs/` | `00-قانون-اساسی-v6.html` | v6 | قانون اساسی جامع و زندهٔ پروژه. سند مادر. |
| `docs/` | `09-قانون-قفل-کنترل-کیفی-v1-LOCKED.html` | v1 · قفل‌شده | ده قفل کنترل کیفی، کارت پیش‌پرواز و قاعدهٔ عدم پس‌رفت. |
| `docs/` | `12-کوئری‌مپ-چاپ-v5.html` | v5 | کوئری‌مپ صفحهٔ چاپ. سه خوشهٔ کوئری، واژگان بازار، رقبا و شکاف‌ها. |
| `spec/abs-v4.2/` | بستهٔ ۲۹ فایلی | v4.2 · قفل‌شده | تنها مرجع اجرا برای عباس. قرارداد Route، معماری، CMS، OTP و پذیرش. |
| `pages/` | `services-urban-investment-v15.html` | ↯ نگاه کنید به یادداشت | صفحهٔ سرمایه‌گذاری و مشارکت در تبلیغات محیطی. |
| `pages/` | `services-printing-v1.html` | v1 | **هاب چاپ**، مسیر کانونیکال `/services/printing/`. حکم: `CONDITIONAL`. |
| `pages/` | `strabord-v6.html` | v6 | استرابورد، با رفع پنج ایراد بصری و دسترس‌پذیری v5. |
| `reports/` | `RPT-کارت-پیش‌پرواز-services-urban-investment-v12.html` | برای v12 | کارت پیش‌پرواز تکمیل‌شدهٔ صفحهٔ بالا. |
| `reports/` | `RPT-route-audit-services-urban-investment-v15.md` | برای v15 | ممیزی مسیر صفحهٔ بالا در برابر قرارداد v4.2. **حکم: BLOCKER.** |
| `reports/` | `RPT-services-printing-v1.md` | برای v1 | کارت پیش‌پرواز، تحقیق، QC و مدرک تست هاب چاپ. |
| `reports/` | `evidence-printing-v1/` | برای v1 | اسکرین‌شات‌های مرورگر واقعی هاب چاپ. |
| `tools/` | `route_audit.py` | — | ممیزی Exact-route. خروجی ۰ = PASS، ۱ = BLOCKER. |
| `tools/` | `qa_page.mjs` | — | QA مرورگر: هفت ویوپورت، زوم ۲۰۰٪، کنتراست، هدف لمسی، تلهٔ فوکوس. |

## سلسله‌مراتب اسناد

سند ۰۰ بالادست همه است. سند ۰۹ بازوی اجرایی کنترل کیفی است و بدون
«کارت پیش‌پرواز» تکمیل‌شدهٔ آن، هیچ خروجی تحویل نمی‌شود. سند ۱۲ ورودی
کوئری برای صفحهٔ چاپ است و پیش از ساخت آن صفحه نوشته شده.

```
00 قانون اساسی  (چه بنویسیم و چرا)
      │
      ├── 09 قفل کنترل کیفی  (چطور جلوی تخطی گرفته شود)  ← دروازهٔ محتوا
      │
      └── 12 کوئری‌مپ چاپ  (ورودی کوئری برای صفحهٔ /printing)
                │
                └── pages/  (خروجی ساخته‌شده)  ← reports/ شاهد عبور از دروازه
                          │
spec/abs-v4.2/  ─────────┘   ← دروازهٔ فنی: قرارداد Route و معماری
   (تنها مرجع اجرا؛ v3، v3.1، v4 و v4.1 منسوخ‌اند)
```

**دو دروازهٔ مستقل، و هر دو الزامی‌اند.** سند ۰۹ می‌گوید محتوای صفحه
قابل تحویل است یا نه. بستهٔ v4.2 می‌گوید مسیرها و لینک‌های همان صفحه با
قرارداد می‌خوانند یا نه. عبور از یکی، عبور از دیگری نیست:
`services-urban-investment` از دروازهٔ محتوا با ۲۲ از ۲۴ گذشت، ولی در
دروازهٔ فنی BLOCKER گرفت.

### بستهٔ v4.2 — نکات قفل‌شده

- ورود با موبایل و OTP از نسخهٔ اولیه؛ حساب پایه: Overview, Orders,
  Invoices, Profile, Logout.
- Ticket/Chat/File/Approval/CRM/Rental اکنون ساخته **نمی‌شوند**.
- معماری تا رونمایی و پایان چرخهٔ ۳۰ روزه قفل است.
- هیچ لینک داخلی هوم نباید خارج از Route Contract باشد.
- ایدهٔ جدید فقط وارد بک‌لاگ می‌شود و برنامهٔ عباس را تغییر نمی‌دهد.

صحت بسته بررسی شد: هر ۲۸ چک‌سام `abs_18_SHA256SUMS_v4_2.txt` تأیید شد و
QC داخلی ۱۴ از ۱۴ PASS است با حکم `PASS_REPLACE_V4_1`.

## یادداشت‌های ثبت‌شده در زمان ایجاد مخزن

**۱· ناسازگاری شمارهٔ نسخهٔ صفحهٔ سرمایه‌گذاری.** فایل با نام `v15` تحویل شد،
اما کامنت بالای خودِ فایل می‌گوید `نسخه: v14` و در سطر «فایل:» نوشته
`services-urban-investment-v13.html`. نام فایل در مخزن `v15` گذاشته شد چون
تازه‌ترین نشانه است. پیش از نسخهٔ بعد باید یکی از این سه درست شود.

**۲· بلاک‌کننده‌های باز.** طبق کارت پیش‌پرواز، دروازهٔ صفحهٔ سرمایه‌گذاری
«باز است، مشروط» با نتیجهٔ ۲۲ از ۲۴:

- **بلاک‌کننده (قفل ۹):** عکس واقعی کارگاه تهران و چاپخانهٔ زرین‌شهر. تا رسیدن
  عکس، بخش اثبات فقط متنی است. در فایل با نشانهٔ `BLOCKER-KARNAMEH` علامت خورده.
- **نیازمند تأیید مالک:** شمارهٔ پروانهٔ `۱۴۰۲۰۳۴۷` که از سند وضعیت پروژه
  برداشته شده و در صفحه درج شده است.

**۳· دو پیشنهاد اصلاح قفل، منتظر تصمیم مالک.** هر دو ردیف «مشروط» کارت
پیش‌پرواز، ایرادِ خودِ قفل تشخیص داده شدند نه ایرادِ صفحه:

- **قفل ۱ (نسبت «شما» به «ما»):** اندازه‌گیری ۴۹ به ۳۰ یعنی ۱٫۶۳ که زیر سقف
  ۲ به ۱ است. علت ساختاری است — جدول‌های قرارداد دوطرفه ذاتاً «ما» دارند.
  پیشنهاد: سقف ۱٫۵ به ۱ برای صفحات قرارداد دوطرفه.
- **قفل ۶ (نسبت پله‌های تایپوگرافی):** دو پلهٔ اول قبول‌اند (۱٫۵۴ و ۱٫۴۴)
  ولی پلهٔ H3 به متن ۱٫۱۸ است. پیشنهاد: پله‌های عنوان ۱٫۳۵ تا ۱٫۵۵، و پلهٔ
  H3 به متن ۱٫۱۵ تا ۱٫۲۵.

**۴· ممیزی مسیر: BLOCKER انتشار.** صفحهٔ سرمایه‌گذاری ۴۲ مسیر ثبت‌نشده و
۴۱ نقض trailing slash دارد. شرح کامل و هفت تصمیمی که با مالک است، در
`reports/RPT-route-audit-services-urban-investment-v15.md`. خلاصه:
۱۰ مورد فقط trailing slash است، ۲۰ مورد نام کوتاه به‌جای مسیر متعارف، و
۱۱ مورد اصلاً در قرارداد ثبت نشده — از جمله **هر هفت صفحهٔ شهرستان**، که
هیچ namespace شهری در معماری برایشان وجود ندارد.

**۵· هاب چاپ ساخته شد.** سند ۱۲ ورودی کوئری این صفحه بود و صفحه‌اش
وجود نداشت، پس نوبت همین بود. دو تعارض منابع پیش از طراحی حل شد:

- **مسیر.** سند ۱۲ نوشته بود `/printing/`، ولی قرارداد v4.2 همان مسیر
  را «۳۰۱ به `/services/printing/`» ثبت کرده و مگامنوی هوم مصوب هم
  «دیدن همهٔ ۱۶ خدمت چاپ» را به `/services/printing/` می‌برد. هاب روی
  مسیر ثبت‌شده ساخته شد و قرارداد دست نخورد. **ممیزی مسیر PASS است:
  صفر مسیر ثبت‌نشده، صفر نقض trailing slash** — برخلاف صفحهٔ
  سرمایه‌گذاری که با ۴۲ مسیر ثبت‌نشده BLOCKER گرفت.
- **نام فرزندها.** `/printing/banner` و مانندش در قرارداد نیستند؛
  لینک‌ها به مسیر عمیق ثبت‌شده می‌روند ولی برچسب دیدنی با نام محصول
  نوشته شده، طبق تصمیم صریح سند ۱۲.

حکم `CONDITIONAL` است نه `PASS`، با سه علت که هیچ‌کدام با کد حل
نمی‌شود: پروندهٔ پرسونا v2 در پروژه نیست (خود سند ۰۰ نوشته «غایب»)،
عکس واقعی کارگاه و چاپخانه نرسیده، و تحقیق زندهٔ صفحه‌به‌صفحهٔ رقبا در
محیط اجرا مسدود بود. جزئیات در `reports/RPT-services-printing-v1.md`.

**۶· استرابورد v6 ساخته شد.** سه موردی که هنگام ساخت هاب چاپ در
`strabord-v5` گزارش شده بود، در `pages/strabord-v6.html` رفع شد. طبق
قاعدهٔ عدم پس‌رفت، v5 حذف نشد.

**یک تصحیح، پیش از هر چیز.** در یادداشت قبلی نوشته بودم پاپ‌آپ v5 فوکوس
نمی‌گیرد و Tab بیرون می‌زند. **این غلط بود.** آزمون مرورگر روی خودِ v5
نشان داد فوکوس درست وارد دیالوگ می‌شود و تله هم کار می‌کند، چون v5
به‌جای rAF از `setTimeout` با ۱۸۰ms استفاده کرده است. کسی دنبال باگی که
وجود ندارد نگردد.

آنچه واقعاً در v5 هست، شکنندگی است نه خرابی: عنصری که فوکوس می‌گیرد
یعنی `.channel`، خودش `transition:.18s` دارد. شکل کوتاه یعنی `all` و
`all` شامل `visibility` هم می‌شود؛ آن پیوند `visibility` را از لایهٔ
پاپ‌آپ به ارث می‌برد، پس در ۹۰ میلی‌ثانیهٔ اول `hidden` می‌ماند و
`focus` رویش بی‌اثر است. عدد ۱۸۰ فقط چون از ۹۰ بزرگ‌تر بود کار می‌کرد.
در v6 ترنزیشن صریح شد و وابستگی به زمان حذف شد.

دو مورد دیگر واقعاً ایراد بودند و رفع شدند:

- پیکان بازشوندهٔ پرسش‌ها با `border-inline-end` ساخته شده بود، که در
  راست‌به‌چپ به‌جای پایین به پهلو می‌افتد.
- **قرمز سطح `#D01A44` بود، ولی هوم مصوب `#DA1E49` را دوازده بار به‌کار
  برده.** گزارش `RPT-strabord-v4-design.md` نوشته «قرمز برند برای سطح
  دست‌نخورده ماند»، ولی مقدارش با شِل یکی نیست؛ آن جمله دقیق نبوده.
  طبق بند ۷ این انشعاب تأییدنشدهٔ شِل است و اندازه‌گیری نشان داد
  بهانه‌ای هم ندارد: متن سفید روی `#DA1E49` نسبت ۴٫۹۳ به ۱ می‌دهد و رد
  می‌شود. قرمزِ **متن** روی `#C11539` ماند، چون `#DA1E49` به‌عنوان متن
  ۴٫۴۵ می‌دهد و زیر کف است. هفت جای SVG هم یکسان شد.

**و دو ایراد که بازآزمایی v5 پیدا کرد و خواسته نشده بودند:**

- **تیتر «پیکربندی و برآورد» نامرئی بود.** قاعدهٔ `on-dark` رنگ سفید
  می‌داد و روی کارت سفیدِ پیکربند هم می‌افتاد؛ نسبت کنتراست دقیقاً
  ۱ به ۱، یعنی متن روی هم‌رنگ خودش. v4 همین استثنا را برای `.fine`
  گذاشته بود ولی برای تیتر نه، و از v4 تا v5 دوام آورد چون هیچ گزارشی
  تیترها را جداگانه نسنجیده بود.
- **سرریز افقی ۳۶ پیکسلی در ۳۲۰px.** برچسب بلند دعوت با
  `white-space:nowrap` پهن‌تر از صفحه می‌شد. گزارش v5 سرریز را «صفر در
  هر چهار ویوپورت» اعلام کرده بود، ولی آن چهار تا ۱۴۴۰ و ۱۰۲۴ و ۳۹۰ و
  ۳۶۰ بودند؛ **۳۲۰ که در فهرست اجباری هست اصلاً آزمایش نشده بود.**

هر دو در v6 رفع شدند. سنجه‌های v6 برابر یا بهتر از v5 است و هیچ‌کدام
پایین نیامد.

**پیشوندهای وبکیت هم اضافه شدند.** اولش سه تا شمرده بودم؛ در عمل چهار
تا بود و نوار چسبان از قلمم افتاده بود: `backdrop-filter` روی هدر، نوار
چسبان و تب‌بار، به‌علاوهٔ `mask-image` روی زمینهٔ کاغذ شطرنجی. بدون
پیشوند، سافاری ماسک را نادیده می‌گیرد و شبکه به‌جای محو شدن تمام سطح
تیره را می‌پوشاند. **این‌ها در سافاری آزمایش نشدند** چون در این محیط
فقط کرومیوم هست؛ آنچه تأیید شد این است که ترتیب اعلان‌ها درست است
(پیشوند اول، استاندارد آخر)، کرومیوم همچنان `blur(16px)` را اعمال
می‌کند، و هیچ سنجه‌ای تغییر نکرد.

**۷· صف بعدی** (از کارت پیش‌پرواز):

- ساخت PDF نمونهٔ ساختار قرارداد مشارکت، پس از دریافت ساختار متعارف قرارداد.
  توجه: قفل ۳ همین حالا به این فایل تکیه کرده، ولی فایل نه ساخته شده و نه
  در قرارداد Route ثبت است.
- هم‌راستاسازی مگامنو با ۶ دستهٔ صفحهٔ خانه — نیازمند فایل نسخهٔ ۱۰ صفحهٔ خانه.
- ثبت این نسخه و سند ۰۹ در فهرست زندهٔ تغییرات.

## قاعدهٔ نام‌گذاری

نام فایل‌ها همان نامی است که خودِ اسناد در کامنت بالای خود به کار برده‌اند،
چون اسناد به یکدیگر با همین نام‌ها ارجاع می‌دهند. شمارهٔ سند و شمارهٔ نسخه
هر دو در نام می‌مانند؛ نسخهٔ جدید فایل جدید می‌سازد و نسخهٔ قبلی حذف نمی‌شود
(قاعدهٔ عدم پس‌رفت، سند ۰۹ تب ۴).
